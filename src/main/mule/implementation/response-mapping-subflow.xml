<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <!-- =========================================================
         Response Mapping Subflow
         Normalizes responses and maps to NotificationResponse
         ========================================================= -->
    <sub-flow name="response-mapping-subflow" doc:name="Response Mapping Subflow">
        
        <logger level="DEBUG" 
                doc:name="Log Response Mapping Start" 
                message='#["[" ++ vars.correlationId ++ "] Mapping response"]' 
                category="${logging.category}"/>
        
        <!-- Transform to NotificationResponse -->
        <ee:transform doc:name="Transform - Notification Response">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
    notificationId: vars.notificationResponse.notificationId default ("NOTIF-" ++ uuid()),
    policyId: vars.originalRequest.policyId,
    status: "SENT",
    message: if (vars.documentGenerated default false)
        "Notification and documents sent successfully"
    else
        "Notification sent successfully"
}]]></ee:set-payload>
            </ee:message>
        </ee:transform>
        
        <logger level="DEBUG" 
                doc:name="Log Response Mapping Complete" 
                message='#["[" ++ vars.correlationId ++ "] Response mapping complete"]' 
                category="${logging.category}"/>
        
    </sub-flow>

</mule>
